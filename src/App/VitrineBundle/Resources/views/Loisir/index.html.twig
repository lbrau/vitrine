{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Loisir list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Type</th>
                <th>Description</th>
                <th>Inscription</th>
                <th>Visible</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('loisir_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.description }}</td>
                <td>{% if entity.inscription %}{{ entity.inscription|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.visible }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('loisir_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('loisir_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('loisir_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
